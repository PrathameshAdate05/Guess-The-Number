<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guesstheno">
    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessTheNo">
        <activity android:name=".Win"
            android:parentActivityName=".StartGame"></activity>
        <activity
            android:name=".MainGame"
            android:label="@string/title_activity_main_game"
            android:theme="@style/Theme.GuessTheNo.NoActionBar"
            android:parentActivityName=".StartGame"/>
        <activity
            android:name=".StartGame"
            android:label="@string/app_name"
            android:theme="@style/Theme.GuessTheNo.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>